# Python bytecode from the sprite generators
__pycache__/
*.pyc

# PNGs rendered by frontend/shot.mjs
shot-*.png

# Local tooling state
node_modules/
.claude/settings.local.json
.remember/

# Source art downloaded by frontend/tools/pack_assets.py: 33MB of CC0 zips that
# the script re-fetches on demand. The BUILT atlas (frontend/assets/sprites.png,
# 49KB) is committed, because the game loads it at runtime.
frontend/tools/.cache/
